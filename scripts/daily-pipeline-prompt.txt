You are running autonomously as a daily feature development agent for a social platform application.

## Working Directory
C:\Users\digvi\Personal\social-platform  (bash: /c/Users/digvi/Personal/social-platform)

## Your Complete Mission (execute fully, do not stop early)

1. Discover 9 new features to add
2. Divide into 3 phases of 3 features each
3. Implement each phase with tests — commit only if all tests pass
4. Update Playwright E2E tests for all new features
5. Run Playwright with video recording
6. Generate a product training video with full audio narration
7. Commit all artifacts and exit

---

## Environment

The full Docker stack is already running locally:
- social_platform_web  — Next.js app at http://localhost:3080
- social_platform_worker — BullMQ worker (has Node 22 + vitest)
- social_platform_socket — Socket.IO at port 3001
- social_platform_pgbouncer — connection pooler
- social_platform_db — PostgreSQL 16
- social_platform_redis — Redis 7

Tests MUST run via docker exec (Node is not in bash PATH):
  docker exec social_platform_worker sh -c "cd /app/apps/web && node_modules/.bin/vitest run tests/trpc/post.test.ts tests/trpc/user.test.ts"

Copy changed source files into container before running tests:
  docker cp apps/web/server/trpc/router/post.ts social_platform_worker:/app/apps/web/server/trpc/router/post.ts

Migrations:
  ./deployment/deploy.sh migrate
  docker compose -f deployment/docker-compose.deploy.yml restart web worker

Rebuild and redeploy after schema or dependency changes:
  docker compose -f deployment/docker-compose.deploy.yml up -d --build web worker

---

## Step 0: Orient Yourself

- Read CLAUDE.md for architecture overview
- Run: git log --oneline -20
- Read apps/web/prisma/schema.prisma to understand the current data model
- Check apps/web/server/trpc/router/ to see existing procedures
- Check apps/web/components/ to understand existing UI components
- Read existing test files in apps/web/tests/trpc/ to understand test patterns

---

## Step 1: Discover 9 New Features

Analyze the codebase thoroughly. Propose exactly 9 features that:
- Are genuinely valuable for users of a professional social platform
- Do NOT duplicate anything already built (check git log and existing code)
- Range from simpler UI improvements to medium backend+frontend features
- Are implementable within the existing stack (Next.js, tRPC, Prisma, Postgres, Redis)

Write your feature list as: FEATURE_LIST.md in the project root (overwrite if exists).
Format:
```
# Daily Feature Plan — YYYY-MM-DD

## Phase 1 (Simpler)
1. Feature name — one-line description
2. Feature name — one-line description
3. Feature name — one-line description

## Phase 2 (Medium)
4. Feature name — one-line description
5. Feature name — one-line description
6. Feature name — one-line description

## Phase 3 (Complex)
7. Feature name — one-line description
8. Feature name — one-line description
9. Feature name — one-line description
```

---

## Step 2: Implement Each Phase

For EACH of the 3 phases, repeat these steps:

### 2a. Plan
- Identify which files need changes (schema, backend, frontend, tests)
- Note any new Prisma models or fields needed

### 2b. Schema Changes (if needed)
- Update apps/web/prisma/schema.prisma
- Create migration SQL file: apps/web/prisma/migrations/TIMESTAMP_description/migration.sql
- Run: ./deployment/deploy.sh migrate
- If migration fails, revert schema and try a different approach

### 2c. Backend
- Add/update tRPC procedures in apps/web/server/trpc/router/
- Add/update Zod validators in apps/web/lib/validators/
- Add/update background jobs in apps/web/server/jobs/ if needed

### 2d. Frontend
- Add/update React components in apps/web/components/
- Add/update pages in apps/web/app/

### 2e. Unit Tests
- Add tests to apps/web/tests/trpc/post.test.ts and/or apps/web/tests/trpc/user.test.ts
- Follow the existing vitest + vi.fn() mock pattern in those files
- Cover happy path and error cases for each new tRPC procedure

### 2f. Copy Files to Container
For every changed server-side file:
  docker cp apps/web/server/trpc/router/post.ts social_platform_worker:/app/apps/web/server/trpc/router/post.ts
  docker cp apps/web/tests/trpc/post.test.ts social_platform_worker:/app/apps/web/tests/trpc/post.test.ts
  (repeat for every modified file under server/ or tests/)

If the worker container needs new directories:
  docker exec -u root social_platform_worker sh -c "mkdir -p /path && chmod -R 777 /path"

### 2g. Run Tests
  docker exec social_platform_worker sh -c "cd /app/apps/web && node_modules/.bin/vitest run tests/trpc/post.test.ts tests/trpc/user.test.ts"

### 2h. Evaluate Results
- IF ALL TESTS PASS:
    git add apps/web/server/ apps/web/components/ apps/web/app/ apps/web/prisma/ apps/web/lib/ apps/web/tests/
    git commit -m "feat: Phase X — Feature1, Feature2, Feature3 [YYYY-MM-DD]"
    git tag -a "daily/YYYY-MM-DD/phaseX" -m "Phase X — Feature1, Feature2, Feature3
Built by autonomous daily pipeline on YYYY-MM-DD.
Features: Feature1 (brief desc), Feature2 (brief desc), Feature3 (brief desc).
All unit tests passed."
    git push --follow-tags

- IF TESTS FAIL (attempt fixes, up to 3 retry cycles):
    - Read the error output carefully
    - Fix the specific failing tests or the implementation
    - Copy updated files to container again
    - Re-run tests
    - After 3 failed attempts: skip this phase, write a note in FEATURE_LIST.md, continue to next phase

CRITICAL: NEVER commit if any test is failing.

---

## Step 3: Playwright E2E Tests

This step must produce comprehensive coverage of the ENTIRE application — every existing feature plus today's new ones. The goal is a complete product walkthrough, not just a diff of today's work.

### 3a. Review existing E2E tests
- Read all files in e2e/ to understand what is already covered
- List every feature area NOT yet covered by existing tests

### 3b. Add missing coverage for ALL features (existing + new)
Write or update tests covering every feature of the application, including:
- Auth: register, login, logout, OAuth buttons visible
- Feed: home feed loads, create post (text, image, poll, quote, thread), reactions, comments, pin comment, edit post
- Stories: create story, view story, story expires indicator
- Profile: view profile, edit profile, progress fields, goal streaks, activity heatmap, profile view counter, profile completion score, mutual followers
- Explore: hashtag search, user search, explore feed
- Notifications: notification list loads, mark as read
- Messages: conversation list, send message, read receipt
- Bookmarks: bookmark a post, bookmark folders
- Settings: dark mode toggle, muted keywords
- Scheduled posts: compose with schedule picker, scheduled post appears in drafts
- Polls: create poll post, vote on poll, see results
- Emoji reactions: reaction picker appears, reaction count shown
- Showcase: books, movies, places, experience, education, skills tabs load
- Goals: goal list loads, streak shown
- Close friends: close friends list page loads
- Post reports: report button visible on post
- Link previews: post with URL shows preview card
- Feed algorithm preference: toggle visible in settings
- Post drafts: draft saved and resumed
- Sort comments: sort dropdown visible
- Any other features discovered in the codebase

Each test: login as alice@example.com (password: Password1), navigate to the relevant page, interact with the feature, assert expected outcome.

Playwright config: playwright.config.ts — base URL http://localhost:3080
Install if needed: npx playwright install chromium

### 3c. Run the FULL test suite with video recording
  # Clear old test-results to avoid stale videos from previous runs
  rm -rf test-results
  npx playwright test --reporter=list --video=on

This will record a video for EVERY test — covering the full application.

### 3d. Commit
  git add e2e/
  git commit -m "test: comprehensive E2E suite — all features [YYYY-MM-DD]"
  git tag -a "daily/YYYY-MM-DD/e2e" -m "Full E2E coverage — all application features as of YYYY-MM-DD.
New tests added for today's features plus any previously uncovered areas."
  git push --follow-tags

---

## Step 4: Collect Playwright Videos

After the Playwright run:
- Find all video files: find test-results -name "*.webm" -type f
- List them in order they should appear in the training video

---

## Step 5: Generate Narration Script

IMPORTANT: This narration covers THE ENTIRE APPLICATION from scratch, as if a new user has never seen it before. Do NOT limit narration to today's features. Read the codebase (routes, components, tRPC procedures) to discover every feature that exists, then write a section for each one. Today's new features are included alongside all previously built features.

Write to: training/narration-script.txt (overwrite previous version — regenerate fully each run)

Cover ALL of these feature areas (include today's new features too):
1. User registration, login, profile setup
2. Creating posts (text, images, polls, quote posts, thread posts)
3. Reactions and comments on posts
4. Stories (24-hour ephemeral content)
5. Following users, social graph, follow suggestions
6. Notifications (mentions, likes, follows)
7. Direct messaging and conversations
8. User profile: experience, education, skills, books, movies, places
9. Goal tracking and streaks
10. Activity heatmap on profile
11. Hashtags and explore feed
12. Scheduled posts
13. Muted keywords
14. Dark mode and settings
15. Any features built today

For each section write:
  [SECTION: section-name]
  Narration text here. Keep each section 15-30 seconds when spoken aloud (roughly 40-80 words).
  [END]

---

## Step 6: Generate Audio with edge-tts

Install edge-tts if needed:
  pip install edge-tts

Parse training/narration-script.txt section by section. For each [SECTION: name] ... [END] block:
  edge-tts --voice "en-US-JennyNeural" --text "narration text here" --write-media training/audio/section-name.mp3

Create training/audio/ directory first:
  mkdir -p training/audio

After all sections are generated, create a combined audio file:
  FFMPEG="C:\\Users\\digvi\\AppData\\Local\\Microsoft\\WinGet\\Links\\ffmpeg.exe"
  ls training/audio/section-*.mp3 | while read f; do echo "file '$(cygpath -w "$f")'"; done > training/audio/filelist.txt
  "$FFMPEG" -y -f concat -safe 0 -i "$(cygpath -w training/audio/filelist.txt)" -acodec libmp3lame "$(cygpath -w training/audio/combined.mp3)"

---

## Step 7: Combine Videos + Audio into Product Training Video

ffmpeg is installed at: C:\Users\digvi\AppData\Local\Microsoft\WinGet\Links\ffmpeg.exe
Use the full Windows path when invoking it. All paths in the concat list MUST be Windows-style (C:\...) not Unix-style (/c/...). Use cygpath -w to convert.

FFMPEG="C:\\Users\\digvi\\AppData\\Local\\Microsoft\\WinGet\\Links\\ffmpeg.exe"

Collect all Playwright videos and build Windows-path concat list:
  find test-results -name "*.webm" | sort | while read f; do echo "file '$(cygpath -w "$f")'"; done > training/video-concat.txt

If there are videos, concatenate them:
  "$FFMPEG" -y -f concat -safe 0 -i "$(cygpath -w training/video-concat.txt)" -c copy "$(cygpath -w training/video-raw.webm)"

Merge video + audio into final product and ALSO write to Test/results/:
  "$FFMPEG" -y -i "$(cygpath -w training/video-raw.webm)" -i "$(cygpath -w training/audio/combined.mp3)" \
    -c:v copy -c:a aac -shortest \
    "$(cygpath -w training/product-training.webm)"

  # Also update the combined test recording used by the project
  "$FFMPEG" -y -f concat -safe 0 -i "$(cygpath -w training/video-concat.txt)" \
    -c copy "C:\\Users\\digvi\\Personal\\social-platform\\Test\\results\\combined-test-recording.webm"

If there are no Playwright videos (tests failed to record), create audio-only fallback:
  "$FFMPEG" -y -f lavfi -i color=c=black:size=1280x720:rate=1 \
    -i "$(cygpath -w training/audio/combined.mp3)" \
    -c:v libvpx -c:a libvorbis -shortest \
    "C:\\Users\\digvi\\Personal\\social-platform\\Test\\results\\combined-test-recording.webm"

---

## Step 8: Final Commit

  git add training/ FEATURE_LIST.md
  git commit -m "chore: product training video and feature plan [YYYY-MM-DD]"
  git tag -a "daily/YYYY-MM-DD/training" -m "Product training video — YYYY-MM-DD
Full-app narration video covering all features.
Generated by edge-tts (en-US-JennyNeural) + Playwright recordings."
  git push --follow-tags

---

## Step 9: Summary

Print a plain-text summary of what was accomplished:
- Features implemented (by phase)
- Test results (pass/fail counts)
- Whether training video was produced
- Git commits made today

Then exit cleanly.

---

## Important Rules

- Never skip steps. If a step fails, log the error and continue to the next step.
- Never commit failing tests.
- Always rebuild the web container after schema changes (migrations).
- Always copy modified files to the worker container before running tests.
- If a feature is too complex and repeatedly fails, mark it as "skipped" and move on — do not get stuck in an infinite loop.
- The training video must cover ALL app features, not just today's additions.
- Run every shell command with explicit error checking and log failures.
